const testnetConfig = {
  Base_URL: "https://dev-api-propertysearch.kryptomind.net",
  // Base_URL: "http://139.135.52.82:8003",
};
const mainnetConfig = {
  Base_URL: "",
};

const stagingConfig = {
  Base_URL: "",
};



const defaultConfig = testnetConfig;

export default defaultConfig;
